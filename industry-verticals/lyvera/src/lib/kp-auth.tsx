/** @deprecated Use `@/lib/demo-auth` — kept for backward compatibility. */
export {
  DEMO_EMAIL as KP_DEMO_EMAIL,
  DEMO_DISPLAY_NAME as KP_DEMO_DISPLAY_NAME,
  DemoAuthProvider as KpAuthProvider,
  useDemoAuth as useKpAuth,
  type DemoUser as KpUser,
} from '@/lib/demo-auth';
